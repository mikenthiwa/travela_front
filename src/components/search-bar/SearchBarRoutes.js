const searchBarAllowedRoutes = [
  /requests(\/*)$/,
  /requests\/my-approvals(\/*)$/,
  /requests\/budgets(\/*)$/,
  /requests\/my-verifications(\/*)$/,
  /settings\/roles\/\d+/,
  /trip-planner\/travel-readiness/,
  /settings\/travel-region\/\d+/,
  /requests\/modifications(\/*)$/
];

export default searchBarAllowedRoutes;
